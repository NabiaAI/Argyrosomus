For panns_inference you need `wget` (on MacOS install with `brew install wget`). Also,
you need to copy `class_labels_indices.csv` to `~/panns_data`. 

Requirements:
```txt
panns-inference
audiomentations
```